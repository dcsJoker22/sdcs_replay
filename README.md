# SDCS Campaign Replay Viewer

Interactive web-based replay of [Strategic DCS](https://strategic-dcs.com) campaign sessions. Displays aircraft tracks, ground units, base ownership, kill events, objective progress, and pilot telemetry on a live map.

## Live Demo

Hosted at https://dcsjoker22.github.io/sdcs_replay/ — currently in development.

---

## Stack

| Component | Technology |
|-----------|-----------|
| Parser | Python 3 — converts Tacview `.acmi` files to session JSON |
| Viewer | Single-file HTML + Leaflet.js — no build step |
| Data | Pre-processed JSON files, one per campaign session |

---

## Project Structure

```
sdcs_replay/
├── .claude/                        # Claude project config
├── public/
│   ├── sdcs_replay_v1.52.html      # Viewer (self-contained, versioned)
│   ├── campaigns.json              # Campaign index — generated by build_campaigns.py
│   ├── Logo_4.1.png
│   └── data/
│       └── YYYY-MM-DD CampaignName/
│           └── session_*.json      # Parsed session data
├── parse_acmi.py                   # Converts .acmi files → session JSON
├── build_campaigns.py              # Scans data/ → writes campaigns.json
├── watch_acmi.py                   # Auto-parses new .acmi files on save
├── download_campaign.py            # Downloads campaign files
├── index.html                      # Entry point
├── CLAUDE.md                       # Claude project instructions
├── .gitignore
└── README.md
```

---

## Setup

### Requirements

```bash
pip install watchdog   # out of date - for watch_acmi.py only — no other dependencies
```

### 1. Parse all campaigns (batch)

```powershell
# Run from project root — parses everything in raw/ and writes to public/data/
python parse_acmi.py
```

Or parse a single session:

```powershell
python parse_acmi.py "raw\2026-02-26 Germany\20260226_074617.zip.acmi"
```

Both `.acmi` and `.zip.acmi` input formats are handled. Output goes to:
`public/data/<campaign_folder>/session_<stem>.json`

### 2. Build the campaign index

```powershell
python build_campaigns.py
```

Scans all `public/data/*/session_*.json` and writes `public/campaigns.json`.
Campaign names and theatre IDs are auto-detected from folder names.

### 3. Serve locally

```powershell
python -m http.server 8080 --directory public/
```

Open `http://localhost:8080/index.html`.

---

## Adding a New Campaign

1. `python download_campaign.py` batch download from (https://strategic-dcs.com/tacview/<new campaign>). stores raw acmi files in `raw\<new campaign folder>`
2. at prompt, enter campaign URL
3. `python parse_acmi.py` (batch) or parse individual files
4. `python build_campaigns.py`

Folder name auto-detection:

| Folder contains | Theatre | campaign_id |
|-----------------|---------|-------------|
| `Caucasus` | Caucasus | 190 |
| `Germany` | Germany | 192 |
| `Syria` | Syria | 185 |
| `Gulf` or `Persian` | Persian Gulf | 189 |

---

## Data Pipeline

```
raw/<campaign>/*.zip.acmi
        │
        ▼
  parse_acmi.py
        │
        ▼
public/data/<campaign>/session_<stem>.json
        │
        ▼
  build_campaigns.py
        │
        ▼
public/campaigns.json
        │
        ▼
index.html  (fetches campaigns.json on load)
```

---

## Viewer Features

- Campaign and session selectors (newest session pre-selected)
- SVG aircraft silhouettes, heading rotation, smooth interpolation
- Altitude and Mach telemetry labels
- Air-launched weapon tracks (missiles, bombs, rockets)
- Kill events with explosion animations and PvP highlighting in kill feed
- Ground unit diamonds with fade/hide on `Visible=0` (despawn/death)
- Base and objective ownership tracking via Shelter3 events (FARPs use Shelter3FARP)
- Objective progress bars (blue vs red)
- Player detail panel with flight history and kill log
- Filters: player aircraft, player labels, AI aircraft, AI labels, ground vehicles, ground labels, trails, kills events, bases/FARPs, weapons (player)
- UI scale slider for icon/label size

---

## Versioning

index.html is the latest version. HTML files are versioned (`public\backup\sdcs_replay_v1.52.html`). Never overwrite an existing versioned file — always increment to the next version number. `campaigns.json` and session JSON files are regenerated freely.
